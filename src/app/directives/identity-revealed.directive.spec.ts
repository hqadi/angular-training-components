import { IdentityRevealedDirective } from './identity-revealed.directive';

describe('IdentityRevealedDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityRevealedDirective();
    expect(directive).toBeTruthy();
  });
});
